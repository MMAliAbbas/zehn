
## LogicIgniter QA — current-root MCP blocker follow-up
- Reviewed integration_tests PR #11 (`fix/9-mcp-persona-login-identifiers`, head `586e310`) and PR #8 (`fix/7-mcp-final-readiness-auth-parity`) against Mira invalid_credentials / Diego empty-JWT evidence.
- Local host-native verification on PR #11 showed persona email identifiers now fetch non-empty JWTs for `mira@li.dev`, `carlos@li.dev`, and `diego@li.dev`; slug usernames still return `400 {"error":"invalid_credentials"}`.
- Remaining blocker moved to BFF auth parity: `go test -tags=integration ./tests/mcp -run TestBFFCatalogMCPMetadataIsEntitledAndPubliclySafe -count=1 -v` fails at dev subscribe with `401 {"error":"invalid token"}`.
- Evidence indicates local identity issues tokens with issuer `https://identity.logicigniter.com/realms/logicigniter` while local BFF log shows JWT middleware issuer `http://localhost:8180/realms/logicigniter`; next QA action is coordinate DevOps/Engineering to restart/reconfigure host-native BFF/Keycloak issuer parity, then rerun MCP final-readiness slice before marking PR #11 ready for review.

## QA follow-up — integration_tests PR #11 current-root MCP final-readiness
- Checked out `integration_tests` PR #11 into local branch `qa/pr-11-current-root-mcp` at `586e310cc1a275bf13ea19cb2baaf356087fa1f0` without touching main or production/customer data.
- Verified `go test ./internal/persona -run TestLoginIdentifierUsesSeededEmail -count=1 -v` passes.
- Verified svc-identity returns non-empty JWTs for `mira@li.dev`, `diego@li.dev`, and `carlos@li.dev`; tokens currently have issuer `https://identity.logicigniter.com/realms/logicigniter`.
- Re-ran targeted MCP/BFF readiness slice: `go test -tags=integration ./tests/mcp -run 'TestBFFCatalogMCPMetadataIsEntitledAndPubliclySafe|TestMCPTestSuiteUsesRealPersonas' -count=1 -v`; `TestMCPTestSuiteUsesRealPersonas` passes but `TestBFFCatalogMCPMetadataIsEntitledAndPubliclySafe` fails at BFF dev subscribe with `401 {"error":"invalid token"}`.
- Conclusion: PR #11 resolves Mira invalid_credentials and Diego empty-JWT symptoms, but does not fully clear current-root MCP final-readiness because host-native BFF auth issuer/parity remains blocked. Evidence path: `/Users/aliai/logicigniter/var/qa-evidence/current-root-mcp-pr11-20260511-qa/`.

## PR #11 current-root MCP persona/auth QA follow-up
- Reviewed `/Users/aliai/logicigniter/integration_tests` at `fix/9-mcp-persona-login-identifiers` commit `586e310cc1a275bf13ea19cb2baaf356087fa1f0`.
- Confirmed evidence dir `/Users/aliai/logicigniter/var/verification/issue-9/integration_tests-20260511T120947Z` is visible and records `go test ./... -count=1` + `go build ./...` pass; integration skipped because `integration_tests/scripts/verify-impact.sh` missing/not executable.
- PR #11 aligns persona login identifiers/emails to local seeded `@li.dev` identities and adds unit coverage for LoginIdentifier on Mira/Carlos/Diego. It does not weaken MCP/BFF assertions, but it does not by itself prove full final-readiness because host-native BFF issuer/auth parity still needs rerun after merge/rebase sequencing.

## PR #11 issue #9 QA review — persona login identifiers
- Reviewed `/Users/aliai/logicigniter/integration_tests` PR branch `fix/9-mcp-persona-login-identifiers` at `586e310cc1a275bf13ea19cb2baaf356087fa1f0`.
- Diff aligns integration test personas to live verification seed emails (`@li.dev`) and changes `InitAll` token fetch from lowercase display-name slugs to `Persona.LoginIdentifier()`.
- Evidence: `go test ./internal/persona -count=1 -v` passed; `go test ./internal/... -count=1` passed; `go test -tags=integration ./tests/mcp -run '^$' -count=1 -v` passed; focused BFF MCP slice reached past persona token initialization and failed later at `/api/v1/dev/subscribe` with `401 {"error":"invalid token"}`.
- QA conclusion: issue #9 persona-login mismatch appears resolved for token acquisition/non-empty JWT path; remaining blocker is BFF/identity auth parity, so final-readiness should wait until issuer/token validation config is corrected, then rerun.

## Auth/runtime incident QA classification — 2026-05-11
- Classified prior Mira `invalid_credentials` / Diego empty JWT as identity/persona token-acquisition failures tied to local seeded login identifiers; current integration_tests branch uses persona email identifiers via `LoginIdentifier()` and must prove non-empty JWTs for Mira, Carlos, Diego before BFF checks.
- Classified newer Carlos `401 {"error":"invalid token"}` as BFF JWT validation/config parity failure after identity token issuance, likely issuer/audience/JWKS/runtime mismatch between local identity/Keycloak/BFF.
- Classified `pq: relation "tenants" does not exist` as svc-identity runtime database schema/migration/search_path failure, separate from credential validity and BFF token validation.
- Minimal readiness ladder: DB schema present -> identity token issuance for personas -> BFF accepts Carlos token on dev subscribe -> internal identity subscription endpoints work -> MCP catalog entitlement/lock behavior passes -> targeted MCP final readiness slice can rerun. No production/main/deploy approvals.
